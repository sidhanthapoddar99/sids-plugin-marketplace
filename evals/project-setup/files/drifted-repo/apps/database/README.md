# database — schema owner

Every schema change starts here. One folder per engine.

| Folder | Holds | Applied by |
|---|---|---|
| `postgres/` | Alembic: `alembic.ini`, `migrations/versions/`, its own `pyproject.toml` | `ctl migrate` |
| `neo4j/` | `init.cypher` — constraints and indexes, idempotent | `ctl migrate` |
| `redis/` | `redis.conf` | mounted read-only by `compose.db.yaml` |

Two backends read Postgres, and the schema uses partial and GIN indexes, pgvector and triggers —
so revisions are hand-written, not autogenerated. `ctl migrate new "<msg>"` creates one.
