// Types only this module uses. A type two modules share moves up one scope, to lib/ (a wire shape beside its lib/api domain file);
// @scope/types only when a second app needs it. Never a global src/types.ts.
