// The only door into this feature. Other features import from here, never from the files beside it.
// Inside: components, hooks, state and local types for one product capability.
// May import: api/, stores/, components/ui, lib/, another feature's index.ts.
