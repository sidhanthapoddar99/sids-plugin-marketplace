// modules/home — the assembled "/" screen. The only export a route imports.
// A module owns what it shows: its own components/, functions/ and types.ts, and composes primitives from
// components/ui. It never imports a route, a layout, or another module; a piece two modules need moves up
// (UI to components/, logic to lib/).
