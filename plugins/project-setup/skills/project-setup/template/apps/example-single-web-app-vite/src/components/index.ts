// Shared UI with no business logic, composed from components/ui: a page header, an empty state, a data table shell.
// A component here renders props. It never imports lib/api, a store, a module or a layout.
// Anything a second frontend needs moves to packages/ui.
