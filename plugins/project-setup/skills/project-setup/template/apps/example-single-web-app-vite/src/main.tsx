// The entry. Mounts <RouterProvider router={router}/> with the tree from ./routeTree.gen.ts, and nothing else.
// import "./styles/globals.css" — the one style import. Nothing else imports CSS.
// The router is created here with the generated route tree; a wrong route is a type error, not a 404.
