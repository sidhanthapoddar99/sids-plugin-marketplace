// The signed-in frame: <Sidebar/>, the header, and <Outlet/> for the route's module. No business logic, no data.
// A layout composes primitives from components/ui and renders whatever the route mounts. It never imports a module.
