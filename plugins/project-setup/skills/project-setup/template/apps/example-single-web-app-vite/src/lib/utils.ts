// cn(): clsx + tailwind-merge, the shadcn helper. Same file as packages/ui/src/lib/utils.ts — it moves there when a second frontend arrives.
// lib/utils.ts imports nothing app-internal: no React, no fetch, no module.
