// modules/dashboard — the assembled dashboard screen. Composes its components/ with data from its functions/,
// and mounts its sub-modules (modules/overview) as tabs or panels. Exported once; routes/_app/dashboard.tsx mounts it.
