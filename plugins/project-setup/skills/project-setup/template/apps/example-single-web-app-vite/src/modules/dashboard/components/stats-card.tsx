// A piece only this module uses. Composes <Card> from components/ui with documented variants; raw utilities for layout glue only.
// When a second module needs it, it moves to src/components/ (this app) or packages/ui (shared across frontends).
