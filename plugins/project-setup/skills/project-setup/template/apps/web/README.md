# web — Vite SPA

The product UI, served at `/app`. Static bundle; no server. Talks to `/api` and `/engine` on its own origin.

Run from here: `bun install && bun dev`. Env: `.env` (public `VITE_*` only). Test: `bun test`, e2e in `e2e/`.
