# site — Next.js

Public site with SSR, served at `/`. Server components fetch `api` by service name; the browser uses `/api`.

Run from here: `bun install && bun dev`. Env: `.env` (`NEXT_PUBLIC_*` only). Test: `bun test`.
