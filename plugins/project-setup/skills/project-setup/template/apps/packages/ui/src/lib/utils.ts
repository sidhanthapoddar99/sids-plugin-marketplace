// cn(): clsx + tailwind-merge, the shadcn helper. extendTailwindMerge teaches it the named utilities
// globals.css adds (border-thin / border-thick are widths, duration-fast / duration-normal are durations),
// or a colour class would silently drop them.
