// Example component shape (shadcn new-york): variants differ by fill and border, sizes use the default scale.
// Utilities only: bg-fg-1, border-thin, rounded-md, text-sm. Never var(--…), never a hex, never an arbitrary value.
