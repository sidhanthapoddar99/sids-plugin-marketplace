// common: config loader (config.rs — three env files skip-if-set, config.yaml + config.local.yaml, ${VAR}, ENGINE__ overrides),
// error types, shared domain types. Every other crate depends on this one; it depends on nothing internal.
