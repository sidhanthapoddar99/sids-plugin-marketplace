# cli — Go, Bubble Tea

Terminal UI over the api. Ships as one binary from `ctl build cli` into `bin/`. Not a container.

Run from here: `go run ./cmd/cli`. Config: `~/.config/<project>/config.yaml` + `<PROJECT>_*` env. Test: `go test ./...`.
