// Root layout. import "@scope/ui/globals.css" — the one style import. Renders <MarketingLayout> from layout/marketing around {children}.
// Next.js owns routing through app/; layout/, modules/, components/ carry the same meaning as in every other frontend.
