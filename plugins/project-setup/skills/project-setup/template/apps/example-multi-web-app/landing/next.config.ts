// Next.js static export for the landing site. It owns `/`, so basePath is "" and no prefix key exists.
//   output: "export", trailingSlash: true, images: { unoptimized: true }
// A display name is a literal in the layout, not env. Nothing from .env.* reaches this build.
