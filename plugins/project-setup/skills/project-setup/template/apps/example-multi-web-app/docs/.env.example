# Frontend env. Read only in dev mode (`bun dev`). Gitignored as .env.
# In prod the image is built by `ctl build`, which forwards this file as build args; the output is
# static files behind nginx. Every key here is public. Build constants only. Astro's prefix is PUBLIC_.
PUBLIC_SITE_NAME=<display name>
