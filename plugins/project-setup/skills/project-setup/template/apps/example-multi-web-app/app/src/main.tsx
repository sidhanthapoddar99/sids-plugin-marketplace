// The entry. Mounts <RouterProvider router={router}/> with the tree from ./routeTree.gen.ts; basepath is WEB_APP_PREFIX (vite `base`).
// import "@scope/ui/globals.css" — the one style import. Nothing else imports CSS.
