// "use client" — this module holds state, so it carries its own client boundary; the page stays a server component.
// modules/dashboard — the assembled screen: its components/ fed by its functions/ and by props from the page.
// Never imports a page, a layout or another module.
