// "/dashboard" — a server component. It may fetch on the server from `http://${API_HOST}:${API_PORT}` (never a NEXT_PUBLIC_ value)
// and passes data to modules/dashboard as props. The module marks itself "use client" if it holds state.
