# Modifier. Publishes every app port to the host. Debug only. Never in prod.
services:
  api:    { ports: ["${API_PORT}:8000"] }
  engine: { ports: ["${ENGINE_PORT}:8080"] }
  site:   { ports: ["${SITE_PORT}:3000"] }
  web:    { ports: ["${WEB_PORT}:80"] }
