# NAME GENERATOR: generate only blank template keys present in .env and unset in the process.
# Active template keys are required after loading; empty process overrides fail validation.
# Absent or commented template keys are inactive; declarations do not add environment keys.
# Generators: hex32 (32 random bytes as hex), password24 (24 alphanumeric characters).
POSTGRES_PASSWORD password24
REDIS_PASSWORD password24
NEO4J_PASSWORD password24
JWT_SIGNING_KEY hex32
ENCRYPTION_KEY_PYTHON hex32
ENCRYPTION_KEY_RUST hex32
