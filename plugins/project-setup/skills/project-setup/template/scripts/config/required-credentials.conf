# One required supplied credential name per line, also declared in .env.template.
# Setup never generates these values. Active generated credentials are already required.
# Add provider-issued credentials here when the project requires them during setup.
