#!/usr/bin/env bash
# ctl build save <name> — docker save every app image into data/test_build/<name>/ with the git sha.
# A frozen snapshot to compare against, or to load on a host without a registry.
