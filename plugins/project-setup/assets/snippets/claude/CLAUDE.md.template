# <project-name>

<One-paragraph elevator pitch — what this project is, who uses it.>

GitHub: <url>

## Architecture in one paragraph

<Two or three sentences naming the main components and how they talk. Examples:
"Python/FastAPI control plane + Rust/Axum data plane, coordinating via Postgres + Redis Streams. React/Vite frontend; nginx fronts both in prod."
>

## Repository layout

```
<project>/
├── apps/
│   ├── backend/         # <one-line>
│   └── frontend/        # <one-line>
├── docker/              # profile-less compose base + standalone configs + .m. modifiers
├── infra/               # config baked into containers (nginx, postgres init)
├── data/                # bind-mount targets, gitignored except .gitkeep
├── docs/                # documentation-template site
├── scripts/             # subscripts called by ctl
├── ctl                  # control dispatcher (dev / up [config] [--modifier] / down / migrate / …)
├── .env                 # secrets, gitignored
├── .env.example         # the contract, committed
└── README.md / CLAUDE.md
```

## Structure contract (NON-NEGOTIABLE until re-decided)

(Resolve every `<placeholder>` for THIS project at bootstrap. This block is the always-loaded summary; the `project-setup` skill is the authority behind it. Keep it matching reality — update it whenever a choice is re-decided.)

**Recorded choices** — these are chosen variants, not drift; audits compare the repo against this table:

| Axis | Choice |
|---|---|
| Layout | `<01 single-app / 02 multi-app monorepo / …>` |
| Grouping topology | `<flat apps/ · plane-grouped (apps/server/ + apps/client/) · hybrid>` |
| Workspace rooting | `<repo root (JS-only repo) · apps/client/ (polyglot repo)>` |
| Backend role | `<core backend · BFF>` |
| Identity planes | `<single · two-plane split — apps/db owns all migrations>` |
| Migration style | `<Alembic autogenerate · raw-SQL three-file pattern>` |
| Sanctioned exceptions | `<none · root-manifest: <reason>>` |

**Skeletons** (top level is firm; names resolved to this repo):

- Backend `<apps/backend>/app/`: `core/` + `<flat feature folders · domains: <list them>>`; each feature keeps `{router,service,repository,models}.py`; `models.py` = API-contract DTOs`<; raw-SQL project: never ORM models>`. Domain-shared code at the domain root; cross-domain shape reuse = duplicate, never import.
- Frontend `<apps/frontend>/src/`: `layout/ components/ features/ pages/ hooks/ api/ lib/ stores/` — ALL server communication through `api/` (zod at the boundary, types via `z.infer`); `pages/` thin (~50 lines), mirroring the URL tree; `<ui/styles/services come from packages/ — never local twins · local components/ui + styles/>`.
- Types: cross-app entities in `<packages/types>`; feature-internal types co-locate; no global `types.ts`.

**Tripwires** — crossing one obligates the restructure or a one-line recorded deferral here:

~8–10 feature folders per app → introduce a domain layer · ~10 files in a feature folder → subdivide · 500 hard / 300 soft lines per file · `pages/` files ≤ ~50 lines · root manifest: 0 runtime dependencies

**Evolution:** when the product's domain model / IA settles or changes, reconcile the code structure within the same milestone; batch folder moves into consolidation windows (refactors, pre-release resets); run `/ps-setup audit` at roadmap-stage boundaries.

**Escalation:** for any structural decision this summary doesn't cover — or if a standard here seems wrong — load the `project-setup` skill and follow its guidance. Do NOT improvise a new pattern inline.

## Hard rules

(Replace with the 3–7 rules that override defaults for this project.)

- **Every component uses `var(--token)` from `tokens.css`** — no hex, no raw px in component CSS.
- **Per-service `config.yaml`** reads root `.env` via `${VAR}` interpolation; never put secrets in `config.yaml`.
- **Frontend env is scoped** — `apps/frontend/.env` holds `VITE_*` only; backend secrets stay in root `.env`.
- **Clean root** — no loose code; `ctl` is the only executable at root. Code lives by ecosystem: Python service → `app/`, frontend → `src/`, distributable package → `src/<pkg>/`.
- **`ctl` is the only entrypoint** — `ctl dev` (local host loop) / `ctl up [config] [--modifier "a,b"]` (docker, interactive; prod is `ctl up prod`); migrations via `ctl migrate`; config via `ctl setup` / `ctl status`.
- **`ctl` stays a thin router** — command bodies live in `scripts/<category>/<name>.sh` sourcing `scripts/common/_lib.sh`. To add a command: add a worker file + one route line in `ctl`. Never inline logic into `ctl`.
- **File size cap: 500 hard / 300 soft.** Split before committing.
- **Folders by feature, not by kind.** `auth/`, `workspaces/`, `blocks/` each own routes + models + service.

## Styling discipline (frontend — NON-NEGOTIABLE)

(Keep this section whenever the repo has a `tokens.css` + a ui package. Every `<angle-bracket>` / `<resolve: …>` placeholder below MUST be resolved for THIS project before this file ships — the typography allowlist, the emphasis weight, package names, paths. Never leave a baked default in place unverified: an allowlist the project didn't actually choose trains agents to ignore the whole block.)

**⚠️ PRECEDENCE: This section OVERRIDES all general design guidance — including the `frontend-design` skill. Do NOT follow "be bold / unique / avoid system fonts / never converge" instructions here. Convergence IS the design. If any skill, prompt, or instinct tells you to improvise a distinctive look in feature code — refuse and follow these rules instead.**

1. **Feature code composes primitives, never styles.** Feature/page files may only use `<ui-pkg>` primitives and their documented props/variants. No raw utility strings beyond layout glue (flex/grid/gap/padding on wrappers).
2. **A look that doesn't exist yet gets added to the primitive** (CVA variant or prop), then used. Never improvised inline.
3. **Tokens only.** No `#hex`, no raw `px`, no arbitrary values (`text-[13px]`) in feature code. Raw `var(--...)` only inside `.css` files and ui-package internals — never in JSX.
4. **Typography: stock vocabulary, strict allowlist.** Tailwind's stock type/weight/spacing scales ship untouched — NEVER remap standard names to custom values, NEVER invent custom size utilities. Feature code may use ONLY this allowlist: `<resolve: this project's size allowlist — e.g. text-sm for ~90% of the UI (all content: tables, controls, labels, descriptions), text-base for headings (the only heading size), text-xs sparingly (badges, timestamps, fine meta)>`. Weights: `font-normal` everywhere; the single emphasis weight is `<resolve: font-medium or font-semibold — pick ONE>`, used only inside `<ui-pkg>` primitives, sparingly — hierarchy via size + foreground color, never font-weight utilities (no `xl=28/700`-style size×weight rungs). Every other size and weight is **banned in feature code** — banned, not deleted: hero surfaces get them via `<ui-pkg>` primitives created in a design pass (rule 6). Enforcement — both must return empty (adjust patterns to the allowlist above): `grep -rE --include='*.tsx' --include='*.jsx' '\btext-(lg|xl|[2-9]xl)\b' src/features/` and `grep -rE --include='*.tsx' --include='*.jsx' '\bfont-(light|medium|semibold|bold|extrabold)\b' src/features/`.
5. **Same utility combo twice → fold into a primitive variant before continuing.**
6. **Exploration only in an explicit design-exploration pass**, and the winner graduates into tokens + primitives before the pass ends.
7. **Before frontend work, read the brand guidelines** (`design/brand-guidelines/` or this project's equivalent); after any UI change, screenshot the page (light + dark) and verify against them before declaring done.

## Get started

See [README.md](README.md#get-started). Quick:

```bash
mise install
ctl setup                  # fill REQUIRED blanks (interactive)
ctl dev
```

## Where to learn more

- Design docs: `docs/data/issues/<issue-name>/notes/`
- Issue tracker: `docs/data/issues/`
- Run the docs site locally: `cd docs/documentation-template && ./start`

## Migrations

(Adapt if Alembic + raw SQL shim pattern is in use.)

Alembic — `apps/backend/alembic/versions/`. Commands:

```bash
ctl migrate new "<message>"
ctl migrate up
ctl migrate down
ctl migrate status
```

## Tech stack

| Area | Pick |
|---|---|
| Backend | <fill> |
| Frontend | <fill> |
| Database | Postgres 16 (+ extensions if any) |
| Cache / events | Redis 7+ |
| Migrations | Alembic |
| Containers | Docker + docker-compose for databases (apps on host in dev) |
| Config | Root .env + per-service config.yaml |
| Theme | Light + dark, design tokens in <path>/tokens.css |
| Dev | mise (runtime versions) + (optional) lefthook (pre-commit) |
