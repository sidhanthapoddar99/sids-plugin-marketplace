# Raw skill test runs. The eval prompts live in evals/<plugin>/evals.json at the repo root;
# the transcripts and outputs from running them are kept here and never committed.
plugins/*/evals-workspace/
